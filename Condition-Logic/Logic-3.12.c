#include <stdio.h>

int main()
{
    char ch = 'a';

    if(ch == 'a' || ch == 'e' || ch =='i' || ch =='o' || ch =='i'){
        printf("%c is a Vowel \n", ch);
    }
    else{
        printf("%c is consonent\n", ch);
    }
    return 0;
}
